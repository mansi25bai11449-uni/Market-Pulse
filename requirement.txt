# MarketPulse Python Toolchain Dependencies
# For PDF report generation, UI screenshots, and PDF inspection tools (/scripts)
#
# Quickstart on a fresh computer / environment:
#   1) pip install -r requirement.txt
#   2) playwright install chromium

# Headless browser automation for terminal screenshots and PDF report generation
playwright>=1.40.0

# PDF inspection, bounding box analysis, and text replacement
pymupdf>=1.24.0
